'use client';

import type { FC } from 'react';

/**
 * AI picture generation used to be a Polotno side-panel section.
 * Stubbed out — CNS editor has no Polotno dependency.
 */
export const PictureGeneratorSection: FC | null = null;
